Elementary Snake game :
* based on typescript
* handling control by keyboard, touchscreen, gyroscope
* Optimized configs for TSlint & Prettier 

todos:
* add tests (as always)
* docker : port 5555 is both in package.json & .env file used by docker-compose